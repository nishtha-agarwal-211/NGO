import 'package:flutter/material.dart';

/// Placeholder — will be built in Milestone 3.
class DonorListScreen extends StatelessWidget {
  const DonorListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Donors')),
      body: const Center(child: Text('Donor list — coming in Milestone 3')),
    );
  }
}
